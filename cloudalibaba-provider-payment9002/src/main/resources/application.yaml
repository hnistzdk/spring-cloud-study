server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848
        server-addr: 192.168.0.112:1111 # nginx暴露

management:
  endpoints:
    web:
      exposure:
        include: '*'
